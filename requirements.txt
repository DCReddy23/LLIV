# Minimal runtime dependencies for this repo.
#
# Notes:
# - For GPU acceleration, install a CUDA-enabled PyTorch build.
# - If you only need CLI/evaluate and not the UI, you can omit gradio.

numpy
PyYAML
Pillow
einops

torch
torchvision

gradio
